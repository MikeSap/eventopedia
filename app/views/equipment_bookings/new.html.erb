<%= form_for @equipment_booking do |f| %>
  <h3>Add Equipment to <%= @show.name %>:</h3>
  <%= f.collection_select :equipment_id, @current_user.vendor.equipment, :id, :name %>
  <%= f.label :quantity%>
  <%= f.number_field :quantity%>
  <%= f.hidden_field :show_id, value: @show.id %>
  <%= f.submit %>
<% end %>